import React from 'react';
import { Text } from 'react-native';

import { DefaultContainer } from 'components';

export default () => (
  <DefaultContainer>
    <Text>Favorite</Text>
  </DefaultContainer>
);
