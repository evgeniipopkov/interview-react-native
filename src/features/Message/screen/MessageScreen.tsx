import React from 'react';
import { Text } from 'react-native';

import { DefaultContainer } from 'components';

export default () => (
  <DefaultContainer>
    <Text>Message</Text>
  </DefaultContainer>
);
